"use client";

// FIN5 — Money Out: composes the existing Expenses page module so the
// sovereign Expenses surface renders inside the Finance shell.
export { default } from "@/app/app/expenses/page";
