"use client";

// FIN5 — Money In: composes the existing Commerce page module so the
// sovereign Revenue surface (quotes, invoices, payments, recurring) renders
// inside the Finance shell with no state duplication.
export { default } from "@/app/app/commerce/page";
