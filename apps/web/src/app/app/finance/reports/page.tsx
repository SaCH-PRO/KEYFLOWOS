"use client";

// FIN5 — Reports: composes the existing Reports page module (P&L, balance
// sheet, executive summary, custom revenue) inside the Finance shell.
export { default } from "@/app/app/reports/page";
