---
kind: journal
cycle: audit
at: 2026-09-03T19:02Z
---

# Audit cycle — 2026-09-03T19:02Z

## Path taken

Direct probe attempted first, per playbook: `curl -m 15 https://keyflowos.com/api/healthz`
failed with `CONNECT tunnel failed, response 403`, confirmed via
`$HTTPS_PROXY/__agentproxy/status` as `connect_rejected` (org policy). Fell
back to CI path (step 1b). This is the 44th consecutive audit with direct
egress blocked — the allowlist fix noted in the playbook (§ Production
reachability, path 1) has not landed yet.

## Findings

- **prod.health.via-ci** (`info`): most recent completed `uptime-monitor.yml`
  run, 33793730425, succeeded at 2026-09-03T18:59:05Z — ~3 minutes before
  this audit ran. Well inside the 30-minute freshness window. Prod healthy.
- **commit-drift** (`info`): most recent completed `deploy-drift.yml` run is
  still 33732471659 (2026-09-03T08:16:29Z, `success`) — the same run cited by
  the 13:02Z audit; no newer run yet (this workflow fires ~daily around
  08:16Z). ~10h45m old, within the 26h staleness window. Prod current/within
  threshold. Issue #69 (opened 2026-08-25 for a real 13-day/64-commit drift)
  remains open awaiting human close — historical, unrelated to this cycle's
  clean result.
- **route-parity.skipped-no-egress** (`info`): CI-fallback path has no
  route-parity substitute (the oracle needs direct egress to hit prod's
  actual routes). Skipped per playbook step 2; not re-filed as
  `probe.oracle-blind` (reserved for the direct path misbehaving, not a
  known egress gap — issue #65 already covers the historical case).
- **diagnostics.unprobed** (`info`): no `ADMIN_BEARER` secret configured;
  also moot this run since direct egress is down anyway.
- **queue.business-events.unprobed** (`info`): `/api/healthz/events` not
  reachable via CI fallback — no queue-depth signal this cycle, same gap as
  every audit on this path.

No `warn`/`fail` findings — nothing escalated, no new GitHub issue opened.
Existing open issues (#64 prod.unreachable, #65 probe.oracle-blind, #69
commit-drift, #70 uptime-monitor.stale, #71 tenant.never_scope, #72
auth.public_handlers) are historical/truth-cycle records and remain open for
human triage; none of them re-fired this cycle.

## Inbox

Wrote `architecture/os/inbox/audit-cycle-2026-09-03.jsonl` (5 `info` lines),
drained it in full this same commit (all `info` → journal only, per
`inbox/README.md` §Triage). No files left over from other producers — the
directory held only `README.md` at run start.

## Corrections

None. Started from a detached HEAD (a prior session's leftover); ran
`git checkout main` then `git status` (which reported "behind origin/main by
23 commits") before touching any files, then `git pull origin main` to fast-
forward — confirmed `HEAD == origin/main` (e1203b3) before reading STATE.md
and the inbox, so no stale-local-main repeat of the 13:02Z cycle's mistake.
