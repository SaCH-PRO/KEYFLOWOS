---
kind: journal
cycle: audit
ran: 2026-09-10T19:03Z
---

# Audit cycle — 2026-09-10 19:03Z

## Step 1: prod health

### 1a — direct probe
`curl -sS -m 15 https://keyflowos.com/api/healthz` → `curl: (56) CONNECT
tunnel failed, response 403`. `$HTTPS_PROXY/__agentproxy/status`
`recentRelayFailures` at the time showed only `mcp.supabase.com:443` (403,
unrelated MCP connector), but the direct CONNECT attempt to
`keyflowos.com:443` itself came back `connect_rejected` — 72nd consecutive
audit citing this gap (unchanged since 2026-08-24T01:02Z). Fell back to 1b.

### 1b — CI fallback
Used `mcp__github__actions_list` (`list_workflow_runs`, same field shape as
`gh run list`). Most recent `status:completed` run of `uptime-monitor.yml`:
`34517888598`, `conclusion:success`, created `2026-09-10T19:01:25Z` —
~1m37s before this cycle started (19:03:02Z), well inside the 30-min
freshness window → `info` `prod.health.via-ci`. That run's `head_sha` is
`ec3ead4cde826b04ce8d3dd001a19a098c5acf04`, an exact match for `origin/main`
HEAD (`git fetch origin main` confirmed the same SHA) — prod healthy,
confirmed indirectly, against the current head exactly.

Commit-drift via CI: most recent `status:completed` run of `deploy-drift.yml`:
`34454222931`, `conclusion:success`, created `2026-09-10T08:15:53Z`,
head_sha `d3185b04` — no drift detected beyond threshold as of that run.
~10h47m old, comfortably inside the 26h staleness window. No newer
deploy-drift.yml run has fired since (daily cadence, ran once today at
08:15Z; next expected ~08:15Z 2026-09-11). `head_sha` no longer matches
current `origin/main` HEAD (`ec3ead4`), but the two commits since
(`96e0543`→`d3185b0` was the last deploy-drift check; `d3185b0`→`ec3ead4`
is this repo's own 13:02Z audit-cycle commit, docs-only, not deployed code)
don't bear on deployed prod code → `info` `commit-drift`, no new drift
signal expected before tomorrow's run.

## Step 2: route parity
CI-fallback path taken in step 1 → route parity has no CI substitute (the
oracle must hit prod's own routes) → skipped entirely, per playbook.
Journaled `info` `route-parity.skipped-no-egress`, not re-filed as
`probe.oracle-blind`.

## Diagnostics / queue
No `ADMIN_BEARER` secret configured (`env | grep -i ADMIN_BEARER` — no
match) → `info` `diagnostics.unprobed`. Direct egress to
`/api/healthz/events` is also blocked, so business-events queue depth has no
signal via either path this cycle → `info`
`queue.business-events.unavailable`.

## Steps 3-5: inbox
Wrote 5 `info` lines to
`architecture/os/inbox/audit-cycle-2026-09-10.jsonl` (`prod.health.via-ci`,
`commit-drift`, `route-parity.skipped-no-egress`, `diagnostics.unprobed`,
`queue.business-events.unavailable`). No other producer files were present
in the inbox at start (only `README.md`). No `warn`/`fail` findings this
cycle — nothing to escalate, so no dedup search against open
`runtime-finding` issues was needed.

Triage: all 5 `info` findings are journal-only per the triage table, with
current values also reflected in the corresponding `STATE.md` §Runtime rows
(unconditional per-cycle tracking, consistent with every prior audit
journal). Drained inbox to zero; inbox file deleted in this commit.

## Corrections
None — clean cycle. Local `main`/`origin/main` briefly looked stale
(11fc669) before this cycle's `git fetch origin main`, which pulled 9 new
commits including 4 audit cycles and 1 truth cycle up through `ec3ead4`; that
was a stale local ref from container start, not a real divergence — no
commits were ever stranded, everything up through `ec3ead4` was already on
GitHub. Direct-egress gap unchanged and already tracked (not re-filed).
