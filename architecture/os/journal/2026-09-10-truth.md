---
kind: journal
cycle: truth
date: 2026-09-10
---

# Truth cycle — 2026-09-10

## Actions

1. `git fetch origin main && git checkout main && git pull --ff-only` — fast-forwarded
   local `main` from `11fc669` (stale local branch, 9 commits behind) to
   `d2d6eda9` (origin HEAD: 4 audit cycles + prior truth cycle's journal
   entries since the session's HEAD was last synced).
2. `pnpm install --frozen-lockfile` — clean.
3. `pnpm --filter @keyflow/db db:generate` — fresh clone had no
   `node_modules/.prisma`; generated in 30.5s per truth.md's standing
   warning about the `--filter build` bypass of turbo's dependency graph.
4. Build: `@keyflow/shared` + `@keyflow/db` + `@keyflow/api` (tsc, all green),
   then `server` (tsc, green).
5. Gate suite: `cd apps/server && pnpm test:unit` (`vitest -c
   vitest.unit.config.ts`) — **373 files / 3,658 tests passed, 0 skipped.**
   Seed `1789024589364` (`--sequence.seed=1789024589364` replays it). Did
   NOT run the bare `npx vitest run` (would pull in the DB/Redis-dependent
   `test/**` suites and false-red as skips per issue #66).
6. Regenerated all four families: `generate-registries.js` (5 files, only
   `generated:` date line moved — confirmed via `git diff`), capability-map
   `generate.js` (byte-identical output to 2026-09-09: 286 flowTools, 207
   cortexCapabilities, 12 domains, 32 targets), `inventory.py` (+5
   files/+407 lines, fully attributed below), `dependency_scan.py`
   (byte-identical to 2026-09-09, no drift).
7. Re-derived every `STATE.md` §Shape row — all 12 counting commands plus
   the two test-count commands matched their `Prev` values exactly (no
   change). `Routes mapped at boot` stays `—` (no docker in this sandbox).
8. `node scripts/os/ledger-sizes.mjs` vs `Prev` — all 14 ledgers checked;
   none moved wrong-direction (see Deltas). No `gate-integrity` issue opened.
9. Gate-file integrity: `git diff --name-only 96e05439..HEAD | grep -E
   '\.(spec|test)\.ts$'` → empty. Zero spec/test files touched since the
   last truth commit, so nothing to intersect with the ledgered gate files.
10. DOC_DEBT sweep: reran both rows' disproving commands.
    - Row 1 (`architecture-risks.md` R3/R21): `BUSINESS_ID_MODELS` is now
      326 entries against a schema scan the doc still cites as "77 of 337".
      The doc body (lines 50, 68) still asserts the stale 77/337/~23%
      figures — only the 2026-08-11 warning banner at the top corrects it.
      Claim is still false. Not a candidate for removal.
    - Row 2 (`docs/CAPABILITY_MAP_2026-08-09.md`): still headlines "245"
      tools and "439" models; actual is 286 tools / 441 models
      (`count-flow-tools.mjs`, schema grep). Claim is still false. Not a
      candidate for removal.
    - No new provably-false doc claim surfaced.
11. Committed to `main`: `architecture/{module,route,event,capability,
    data-ownership}-registry.yaml`, `architecture/inventory.json`,
    `architecture/os/state/STATE.md`, this journal file. Verified via
    `git diff --cached --name-only` before commit that the index held
    exactly these paths (no other session's staged work was present).
    `architecture/dependencies.json` and the capability-map outputs were
    byte-identical to HEAD, so nothing to add for those.

## Deltas

- Server test seed: `1789024589364` (was `1788938278652`) — expected,
  `sequence.shuffle` reseeds every run; test/file counts unchanged
  (3,658/373/0 skipped).
- `architecture/inventory.json`: `total_files` 4313→4318 (+5), `total_lines`
  1,042,476→1,042,883 (+407). Fully attributed to 5 new journal files
  accumulated since the last truth commit `96e05439`:
  `2026-09-09-audit-13.md` (69), `2026-09-09-audit-19.md` (79),
  `2026-09-09-truth.md` (108 — the prior cycle's own journal entry, written
  after that cycle generated its inventory snapshot, so it only shows up as
  new in *this* cycle's diff), `2026-09-10-audit-01.md` (77),
  `2026-09-10-audit-07.md` (74). Sum = 407 lines / 5 files, exact match.
  The `.claude/coordination/sessions/<uuid>.json` entry is a net-zero
  rename (this session's coordination file replacing a prior session's).
  No unexplained movement.
- All 5 registry YAMLs: only the `generated:` date line changed
  (2026-09-09 → 2026-09-10). Zero structural drift.
- Capability map (docs + seed.ts): byte-identical to 2026-09-09's output.
- `dependencies.json`: byte-identical to 2026-09-09's committed file.
- Ledger cardinalities: all 14 rows re-derived, zero wrong-direction
  movement. `tenant.never_scope` (fixed, frozen display `3`) still shows
  real value `10` — unchanged from 2026-09-09's real value, issue #71
  still open, no new drift. `auth.public_handlers` (shrink, frozen display
  `219`) still shows real value `223` — unchanged from 2026-09-09, issue
  #72 still open, no new drift. All other 12 ledgers matched their `Prev`
  exactly.
- Tenant isolation partition: 326 scoped + 13 acknowledged-unscoped + 10
  never-scope (real) = 349, consistent with `tenant-model-list.spec.ts`'s
  partition invariant. Displayed `never_scope` value stays frozen at 3 per
  #71.

## Corrections

- The local `main` branch pointer in this container's checkout was 9
  commits stale (last synced at `11fc669`, the 2026-09-08 truth commit)
  even though `HEAD` was detached at the current `origin/main` tip
  (`d2d6eda9`) from a prior session action. Fast-forwarded the branch
  pointer before starting step 1's build — no functional impact since the
  working tree was already at the correct commit, but worth noting so a
  future cycle doesn't assume `git log main` reflects reality without
  first checking `git status` for a detached HEAD.
- No other surprises this cycle: build, gates, and regeneration all matched
  the documented playbook shape on the first pass.
