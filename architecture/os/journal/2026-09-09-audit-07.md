---
kind: journal
cycle: audit
ran: 2026-09-09T07:02Z
---

# Audit cycle — 2026-09-09 07:02Z

## Step 1: prod health

### 1a — direct probe
`curl -s -m 15 https://keyflowos.com/api/healthz` → `CONNECT tunnel failed,
response 403` (exit 56). Confirmed via `$HTTPS_PROXY/__agentproxy/status`:
`connect_rejected` on `keyflowos.com:443`, "gateway answered 403 to CONNECT
(policy denial or upstream failure)". Egress to prod is still blocked —
66th consecutive audit citing this gap (unchanged since 2026-08-24T01:02Z).
Fell back to 1b.

### 1b — CI fallback
`gh run list --workflow uptime-monitor.yml --limit 5` → most recent
`status:completed` run `34321653461`, `conclusion:success`, created
`2026-09-09T06:59:52Z` — ~2m09s before this cycle started (07:02:01Z), well
inside the 30-min freshness window → `info` `prod.health.via-ci`. Prod
healthy, confirmed indirectly. (All 5 most recent runs in the window were
`success`, all against `head_sha 6a1d7b3e`, the current `origin/main` HEAD.)

Commit-drift via CI: `gh run list --workflow deploy-drift.yml --limit 3` →
most recent `status:completed` run `34203590374`, `conclusion:success`,
created `2026-09-08T08:16:13Z`, `head_sha 11fc6691` — that was
`origin/main` HEAD at the time the workflow ran. ~22h46m old, still inside
the 26h staleness window (next scheduled run expected ~08:16Z today, about
1h14m from now) → `info` `commit-drift`, zero drift, prod current.

## Step 2: route parity

CI-fallback path taken in step 1 → route parity has no CI substitute (the
oracle must hit prod's own routes) → skipped entirely, per playbook.
Journaled `info` `route-parity.skipped-no-egress`, not re-filed as
`probe.oracle-blind`.

## Diagnostics / queue

No `ADMIN_BEARER` secret configured (`env | grep -i ADMIN_BEARER` — no
match) → `info` `diagnostics.unprobed`. Direct egress to
`/api/healthz/events` is also blocked, so business-events queue depth has
no signal via either path this cycle → `info`
`queue.business-events.unavailable`.

## Steps 3-5: inbox

Wrote 5 `info` lines to
`architecture/os/inbox/audit-cycle-2026-09-09.jsonl` (`prod.health.via-ci`,
`commit-drift`, `route-parity.skipped-no-egress`, `diagnostics.unprobed`,
`queue.business-events.unavailable`). No other producer files were present
in the inbox at start (only `README.md`). No `warn`/`fail` findings this
cycle — nothing to escalate, no new GitHub issue needed, no 2-consecutive
escalation triggered.

Triage: all 5 `info` findings are journal-only per the triage table, with
current values also reflected in the corresponding `STATE.md` §Runtime
rows (unconditional per-cycle tracking, consistent with every prior audit
journal). Drained inbox to zero; inbox file deleted in this commit.

## Corrections

None. This cycle's findings don't touch issues #71 (`tenant.never_scope`)
or #72 (`auth.public_handlers`) — truth-cycle territory, not re-checked
here. Local `main` branch was found behind `origin/main` at session start
(HEAD detached at `origin/main`'s tip); reset local `main` to `origin/main`
before committing — no data loss, local `main` carried no unique commits.
Clean cycle overall — prod healthy via CI, commit-drift confirmed fresh
and current, nothing to escalate, direct-egress gap unchanged and already
tracked (not re-filed).
