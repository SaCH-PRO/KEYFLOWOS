---
kind: journal
cycle: audit
ran_at: 2026-09-01T01:02Z
---

# Audit cycle — 2026-09-01T01:02Z

## Prod reachability

Direct probe attempted first: `curl -sS https://keyflowos.com/api/healthz` failed
with `CONNECT tunnel failed, response 403` — confirmed via
`$HTTPS_PROXY/__agentproxy/status` as a `connect_rejected` policy denial on
`keyflowos.com:443`. This is the 33rd consecutive audit with direct egress
blocked. Fell back to CI path per playbook step 1b.

## CI fallback findings

- **prod.health.via-ci** (`info`) — most recent completed `uptime-monitor.yml`
  run (33455283287) succeeded at 2026-09-01T00:34:17Z, ~27m44s before this
  audit — within the 30-min freshness window. Prod healthy, confirmed
  indirectly. This replaces `prod.unreachable`, not a fail.
- **commit-drift** (`info`) — most recent completed `deploy-drift.yml` run is
  still 33372254273 (2026-08-31T08:17:58Z, `success`), unchanged for a third
  consecutive audit; ~16h44m old, still within the 26h staleness window. Prod
  current/within threshold. Issue #69 (from an earlier resolved commit-drift
  finding) remains open awaiting human close — not this cycle's to close.
- **route-parity.skipped-no-egress** (`info`) — CI-fallback path has no
  route-parity substitute (the oracle needs direct egress to prod's own
  routes). Skipped per playbook, not re-filed as `probe.oracle-blind` since
  this is a known, already-diagnosed egress gap.
- **diagnostics.unprobed** (`info`) — no `ADMIN_BEARER` secret configured this
  run; `/api/diagnostics/infrastructure` stays unprobed, recorded rather than
  assumed healthy.

All four findings were `info` severity — drained straight to this journal, no
STATE §Runtime escalation beyond refreshing the standing rows' timestamps, no
issue filed. Inbox file `audit-cycle-2026-09-01.jsonl` processed and deleted
in the same commit as this journal entry.

## Corrections

None — this run matched the CI-fallback shape of the two preceding audits
(13:02Z and 19:01Z on 2026-08-31) with no surprises. Deploy-drift's run age is
creeping toward the 26h staleness threshold (16h44m now vs 10h43m at 19:01Z)
purely because no new `deploy-drift.yml` run has fired since 2026-08-31T08:17:58Z;
worth a note for the next truth cycle if it crosses 26h before a fresh run lands.
