---
kind: journal
cycle: audit
ran_at: 2026-09-01T07:05Z
---

# Audit cycle — 2026-09-01T07:05Z

## Prod reachability

Direct probe attempted first: `curl -sS https://keyflowos.com/api/healthz` and
`.../api/readyz` both failed with `CONNECT tunnel failed, response 403` —
confirmed via `$HTTPS_PROXY/__agentproxy/status` as a `connect_rejected`
policy denial on `keyflowos.com:443` (also seen on `mcp.supabase.com:443`,
unrelated to this run). This is the 34th consecutive audit with direct
egress blocked. Fell back to CI path per playbook step 1b.

## CI fallback findings

- **prod.health.via-ci** (`info`) — most recent completed `uptime-monitor.yml`
  run (33480089899) succeeded at 2026-09-01T07:00:23Z, ~5m22s before this
  audit — well within the 30-min freshness window. Prod healthy, confirmed
  indirectly. This replaces `prod.unreachable`, not a fail.
- **commit-drift** (`info`) — most recent completed `deploy-drift.yml` run is
  still 33372254273 (2026-08-31T08:17:58Z, `success`), unchanged for a 4th
  consecutive audit; ~22h47m old, still within the 26h staleness window
  (threshold ~2026-09-01T10:17:58Z). Prod current/within threshold. Issue #69
  (from an earlier resolved commit-drift finding) remains open awaiting human
  close — not this cycle's to close. `deploy-drift.yml`'s history shows it
  fires roughly once daily around 08:00-12:00 UTC, so a fresh run is expected
  within the next ~1-5h; if it hasn't landed by the 13:01Z audit, that run
  will cross the 26h threshold and should escalate to `warn`
  `deploy-drift.stale` per the playbook.
- **route-parity.skipped-no-egress** (`info`) — CI-fallback path has no
  route-parity substitute (the oracle needs direct egress to prod's own
  routes). Skipped per playbook, not re-filed as `probe.oracle-blind` since
  this is a known, already-diagnosed egress gap.
- **diagnostics.unprobed** (`info`) — no `ADMIN_BEARER` secret configured this
  run; `/api/diagnostics/infrastructure` stays unprobed, recorded rather than
  assumed healthy.

All four findings were `info` severity — drained straight to this journal, no
STATE §Runtime escalation beyond refreshing the standing rows' timestamps, no
issue filed. Inbox file `audit-cycle-2026-09-01.jsonl` processed and deleted
in the same commit as this journal entry.

## Corrections

None — this run matched the CI-fallback shape of the four preceding audits
(13:02Z, 19:01Z on 2026-08-31, and 01:02Z on 2026-09-01) with no surprises.
Deploy-drift's run age continued creeping toward the 26h staleness threshold
(22h47m now vs 16h44m at 01:02Z) purely because no new `deploy-drift.yml` run
has fired since 2026-08-31T08:17:58Z — flagged above with a concrete
threshold-crossing estimate for the next cycle to check first, rather than
re-deriving it from scratch.
