---
kind: journal
cycle: audit
date: 2026-09-05T19:02Z
---

# Audit cycle — 2026-09-05T19:02Z

## Step 1: prod health

Direct probe attempted first (per playbook, not assumed blocked): `curl -sS
-m 15 https://keyflowos.com/api/healthz` → exit 56, `CONNECT tunnel failed,
response 403`. The agent-proxy relay-failure transcript attached to the curl
error confirmed `keyflowos.com:443 — connect_rejected` for this specific
call (the `$HTTPS_PROXY/__agentproxy/status` snapshot itself only showed
unrelated `mcp.supabase.com` entries, same pattern as prior audits — the
curl transcript is the primary evidence). 52nd consecutive audit with direct
egress blocked, since 2026-08-24. Fell back to CI path (step 1b).

- **uptime-monitor.yml**: most recent completed run `33985526731`,
  `createdAt 2026-09-05T18:55:09Z`, `conclusion: success`. Audit ran at
  `19:02:05Z`, so the run is ~7m old — well inside the 30-min freshness
  window → `info` `prod.health.via-ci`. Replaces `prod.unreachable`.
- **deploy-drift.yml** (commit-drift): most recent completed run
  `33954666486`, `createdAt 2026-09-05T08:13:14Z`, `conclusion: success`.
  Same run as the previous audit (13:01Z) — expected, since deploy-drift.yml
  only fires daily at 08:00 UTC and no new run has scheduled since; ~10h49m
  old, still well inside the 26h staleness window → `info` `commit-drift`,
  prod current/within threshold.

## Step 2: route parity

CI-fallback path taken in step 1 → route parity skipped entirely per
playbook (no CI substitute for the direct-egress oracle). Journaled `info`
`route-parity.skipped-no-egress`, not re-filed as `probe.oracle-blind`.

## Step 3-5: inbox

Wrote 5 `info` lines to `architecture/os/inbox/audit-cycle-2026-09-05.jsonl`
(prod.health.via-ci, commit-drift, queue.business-events.unavailable,
route-parity.skipped-no-egress, diagnostics.unprobed — no `ADMIN_BEARER`
secret configured in this environment, confirmed via `env | grep -i
ADMIN_BEARER`, no match). No other producer files were present in the
inbox at start (only `README.md`). All 5 findings are `info` — no `fail`
or `warn`, so no GitHub issue and no escalation check needed (no `check`
id currently has 2 consecutive non-info results — every check has been
`info` for many consecutive cycles). Drained inbox to zero; `STATE.md`
§Runtime rows updated with this run's timestamps; inbox file deleted in
this commit.

Also confirmed the working tree was in a detached-HEAD state at session
start (`HEAD detached from refs/heads/main` at `bd26a0d`, one commit behind
origin's `232b4b0`); re-pointed local `main` at `origin/main` before doing
any work so this run's commit lands on the real branch history.

## Corrections

Issues #71 (`tenant.never_scope`) and #72 (`auth.public_handlers`) were not
re-checked for state changes this cycle (audit playbook doesn't require it;
that's truth cycle's job) — noted only that no new runtime finding touches
either. Direct egress to `keyflowos.com` remains blocked; still the known
2026-08-24 gap awaiting the human egress-allowlist action, not re-raised as
a new finding.
