---
kind: journal
cycle: truth
date: 2026-09-11
---

# Truth cycle — 2026-09-11

## Actions

1. `git fetch origin main` — found `HEAD` detached at `6a66785` (74th audit
   commit) while the local `main` branch pointer was stale at `11fc669` (the
   2026-09-08 truth commit, 14 commits behind). `git checkout main && git
   pull --ff-only` fast-forwarded `main` to `origin/main` (`6a66785`) — no
   functional impact, the working tree was already correct.
2. `pnpm install --frozen-lockfile` — clean. Container ships Node 22 by
   default (`/opt/node22`); used `/opt/node20/bin` (v20.20.2, matches the
   repo's pinned `20.18.1` major) on `PATH` for install/build/test so the
   CommonJS-on-Node-20 constraint (`OS.md` NEVER #6) is actually exercised,
   not just declared.
3. `pnpm --filter @keyflow/db db:generate` — fresh clone had no
   `node_modules/.prisma`; generated in ~14s per truth.md's standing warning
   about the `--filter build` bypass of turbo's dependency graph.
4. Build: `@keyflow/shared` + `@keyflow/db` + `@keyflow/api` (tsc, all
   green), then `server` (tsc, green).
5. Gate suite: `cd apps/server && pnpm test:unit` (`vitest -c
   vitest.unit.config.ts`) — **373 files / 3,658 tests passed, 0 skipped.**
   Seed `1789110975183` (`--sequence.seed=1789110975183` replays it). Did
   NOT run the bare `npx vitest run` (would pull in the DB/Redis-dependent
   `test/**` suites and false-red as skips per issue #66).
6. Regenerated all four families: `generate-registries.js` (5 files, only
   `generated:` date line moved — confirmed via `git diff`), capability-map
   `generate.js` (byte-identical output to 2026-09-10: 286 flowTools, 207
   cortexCapabilities, 12 domains, 32 targets), `inventory.py` (+5
   files/+387 lines, fully attributed below), `dependency_scan.py`
   (byte-identical to 2026-09-10, no drift).
7. Re-derived every `STATE.md` §Shape row — all 12 counting commands plus
   the two supplementary test-count commands (`apps/web` vitest: 210/22
   files; `packages/{db,api}`: 12/2 files, db 7 + api 5) matched their
   `Prev` values exactly (no change). `Routes mapped at boot` stays `—` (no
   docker in this sandbox).
8. `node scripts/os/ledger-sizes.mjs` vs `Prev` — all 14 ledgers checked;
   none moved wrong-direction (see Deltas). No new `gate-integrity` issue
   opened.
9. Gate-file integrity: `git diff --name-only d3185b0..HEAD | grep -E
   '\.(spec|test)\.ts$'` → empty. Zero spec/test files touched since the
   last truth commit, so nothing to intersect with the ledgered gate files.
10. DOC_DEBT sweep: reran both rows' disproving commands.
    - Row 1 (`architecture-risks.md` R3/R21): `BUSINESS_ID_MODELS` is now
      326 entries against a schema scan the doc still cites as "77 of 337".
      The doc body (lines 50, 68) still asserts the stale 77/337/~23%
      figures — only the 2026-08-11 warning banner at the top corrects it.
      Claim is still false. Not a candidate for removal.
    - Row 2 (`docs/CAPABILITY_MAP_2026-08-09.md`): still headlines "245"
      tools and "439" models; actual is 286 tools / 441 models
      (`count-flow-tools.mjs`, schema grep). No superseded banner present
      yet. Claim is still false. Not a candidate for removal.
    - No new provably-false doc claim surfaced.
11. Checked issues #71 (`tenant.never_scope`) and #72
    (`auth.public_handlers`) — both still **open**, no new activity since
    2026-08-31, awaiting human review. Both rows kept their frozen
    Value/Prev (3/3, 219/219) per truth.md step 6.
12. Committed to `main`: `architecture/{module,route,event,capability,
    data-ownership}-registry.yaml`, `architecture/inventory.json`,
    `architecture/os/state/STATE.md`, this journal file. Verified via
    `git diff --cached --name-only` before commit that the index held
    exactly these paths (no other session's staged work was present).
    `architecture/dependencies.json` and the capability-map outputs were
    byte-identical to HEAD, so nothing to add for those.

## Deltas

- Server test seed: `1789110975183` (was `1789024589364`) — expected,
  `sequence.shuffle` reseeds every run; test/file counts unchanged
  (3,658/373/0 skipped).
- `architecture/inventory.json`: `total_files` 4318→4323 (+5), `total_lines`
  1,042,883→1,043,270 (+387). Fully attributed to 5 new journal files
  accumulated since the last truth commit `d3185b0`:
  `2026-09-10-audit-13.md` (71), `2026-09-10-audit-19.md` (74),
  `2026-09-10-truth.md` (102 — the prior cycle's own journal entry, written
  after that cycle generated its inventory snapshot, so it only shows up as
  new in *this* cycle's diff), `2026-09-11-audit-01.md` (71),
  `2026-09-11-audit-07.md` (69). Sum = 387 lines / 5 files, exact match.
  The `.claude/coordination/sessions/<uuid>.json` entry is a net-zero
  rename (this session's coordination file replacing a prior session's).
  No unexplained movement.
- All 5 registry YAMLs: only the `generated:` date line changed
  (2026-09-10 → 2026-09-11). Zero structural drift.
- Capability map (docs + seed.ts): byte-identical to 2026-09-10's output.
- `dependencies.json`: byte-identical to 2026-09-10's committed file.
- Ledger cardinalities: all 14 rows re-derived, zero wrong-direction
  movement. `tenant.never_scope` (fixed, frozen display `3`) still shows
  real value `10` — unchanged from 2026-09-10's real value, issue #71
  still open, no new drift. `auth.public_handlers` (shrink, frozen display
  `219`) still shows real value `223` — unchanged from 2026-09-10, issue
  #72 still open, no new drift. All other 12 ledgers matched their `Prev`
  exactly.
- Tenant isolation partition: 326 scoped + 13 acknowledged-unscoped + 10
  never-scope (real) = 349, consistent with `tenant-model-list.spec.ts`'s
  partition invariant. Displayed `never_scope` value stays frozen at 3 per
  #71.

## Corrections

- Same class of drift as 2026-09-10's entry, worse this time: `HEAD` was
  detached 14 commits behind `origin/main` (not 9), and `main`'s local
  pointer was stale at the 2026-09-08 truth commit rather than the prior
  cycle's start point. `git fetch` + `checkout main` + `pull --ff-only`
  resolved it before any build step ran. A future cycle should treat "am I
  on `main` and is it fast-forwarded" as a zero-cost check to run before
  trusting any local `git log` output, not just at session start.
- Confirmed the container's default `node` (v22, at `/opt/node22/bin`) is
  newer than the repo's pinned engine (`20.18.1`); `/opt/node20` (v20.20.2)
  exists as an install and was put first on `PATH` for every install/build/
  test command this cycle. This is worth calling out explicitly in the
  playbook or `AGENTS.md` (reflect cycle's territory) so future truth
  cycles don't build/test under the wrong Node major by default.
- No other surprises this cycle: build, gates, and regeneration all matched
  the documented playbook shape on the first pass.
