---
kind: journal
cycle: audit
ran: 2026-09-11T13:02Z
---

# Audit cycle — 2026-09-11 13:02Z

## Step 1: prod health

### 1a — direct probe
`timeout 15 curl -sS https://keyflowos.com/api/healthz` → exit 56, `CONNECT
tunnel failed, response 403`. `timeout 15 curl -sS https://keyflowos.com/api/readyz`
→ same. Confirmed via `$HTTPS_PROXY/__agentproxy/status`: direct attempts
logged `keyflowos.com:443 — connect_rejected` this run (the status
endpoint's own `recentRelayFailures` snapshot only showed unrelated
`mcp.supabase.com:443` 403s from an MCP tool, but the live curl attempts
themselves were rejected) — 75th consecutive audit citing this gap,
unchanged since 2026-08-24T01:02Z. Fell back to 1b.

### 1b — CI fallback
`mcp__github__actions_list` (`list_workflow_runs`), same field shape as
`gh run list` (the `gh` CLI itself is not installed in this session — this
tool is the documented substitute). Most recent `status:completed` run of
`uptime-monitor.yml`: `34601512473`, `conclusion:success`, created
`2026-09-11T12:56:05Z` — ~6m before this cycle started, well inside the
30-min freshness window → `info` `prod.health.via-ci`. `head_sha`
`d91b935b0c5f2a4fe9873fe10009d41fcab675ff` is an exact match for
`origin/main` HEAD (`git fetch origin main` confirmed `11fc669..d91b935`,
`git ls-remote origin main` confirmed `d91b935b`) — prod healthy, confirmed
indirectly, against the current head exactly.

**Commit-drift via CI — this cycle's real finding.** Most recent
`status:completed` run of `deploy-drift.yml`: `34578300288`,
`conclusion:failure`, created `2026-09-11T08:15:36Z` (head_sha of the
*checkout* is `d91b935b`, the current main — the drift check itself
compares prod against that checkout). `mcp__github__get_job_logs` on its
single job (`103195742099`, step "Check deploy drift") shows:

```
[drift] prod=2c3da5d4569e status=ahead ahead_by=44 first_missing=2026-09-03T06:32:39Z
[drift] behind by 44 commit(s), ~8 day(s).
##[warning]prod is ~8d behind main (44 commits, oldest 2026-09-03T06:32:39Z) — a deploy has been missed for over 7d
```

`scripts/deploy-drift-check.sh` exited 1 (real drift, not the exit-3
inconclusive/prod-unreachable case — that's mapped to a success with a
warning annotation by the workflow, so a hard `failure` here means the
compare itself succeeded and found drift). Per playbook this is `warn`
`commit-drift`, not `fail` — the deploy-drift job's failure conclusion
signals drift-beyond-threshold, not prod being down (that's uptime-monitor's
job, and it's green).

This is the **1st occurrence** of this recurrence: the prior audit
(07:02Z) read this same check as `info`/success against `d3185b04` with no
drift, and the run before that (2026-09-10 13:02Z run `ec3ead4`) logged
"commit-drift zero, exact head match on both" — so prod was current as
recently as ~29h ago and has since drifted 44 commits / ~8 days behind,
meaning a deploy was expected and missed sometime after main advanced past
prod's current `2c3da5d4569e`. One occurrence does not meet the playbook's
2-consecutive-audit-run escalation threshold, so this stays a `warn` (STATE
row + journal), not a new issue.

Dedup check before deciding: searched open issues for `commit-drift` /
`runtime-finding` and found
[#69](https://github.com/SaCH-PRO/KEYFLOWOS/issues/69) — open since
2026-08-25, titled "commit-drift — prod ~13d/64 commits behind main,
unresolved across 2 consecutive audit runs". Read its body: it documents a
**distinct, earlier** drift episode (prod `ea9a21fc6dca`, 64 commits, ~13
days, first-missing 2026-08-12) that must have been resolved by a
subsequent deploy — every audit between 2026-08-26 and this morning's 07:02Z
run recorded `commit-drift` as `info`/no-drift, most recently against
`d3185b04` with an exact head match. So #69 is stale (its underlying
condition cleared, but the issue itself was never closed — closing issues
isn't in the audit cycle's write matrix, so it wasn't this cycle's place to
close it either). This cycle's drift is a fresh recurrence against a
different prod commit (`2c3da5d4569e`, not `ea9a21fc6dca`) and a different
window (2026-09-03 onward, not 2026-08-12 onward) — not a duplicate of #69,
but also not yet at the escalation threshold on its own, so no new issue was
opened. Noted the overlap in the STATE row and this journal so the next
audit (and any human triaging #69) has the context; #69 itself was left
untouched (issue edits are not in this cycle's write matrix beyond opening
new ones).

## Step 2: route parity
CI-fallback path taken in step 1 → route parity has no CI substitute (the
oracle must hit prod's own routes) → skipped entirely, per playbook.
Journaled `info` `route-parity.skipped-no-egress`, not re-filed as
`probe.oracle-blind`.

## Diagnostics / queue
No `ADMIN_BEARER` secret configured for this routine → `info`
`diagnostics.unprobed` (the `/api/diagnostics/infrastructure` surface,
including `Token Encryption Key Source`, is unprobed this cycle). Direct
egress to `/api/healthz/events` is also blocked, so business-events queue
depth has no signal via either path this cycle → `info`
`queue.business-events.unavailable`.

## Steps 3-5: inbox
Wrote 5 lines (4 `info`, 1 `warn`) to
`architecture/os/inbox/audit-cycle-2026-09-11.jsonl` (`prod.health.via-ci`,
`commit-drift`, `route-parity.skipped-no-egress`, `diagnostics.unprobed`,
`queue.business-events.unavailable`). No other producer files were present
in the inbox at start (only `README.md`).

Triage: the 4 `info` findings are journal-only per the triage table, with
current values also reflected in the corresponding `STATE.md` §Runtime rows
(unconditional per-cycle tracking, consistent with every prior audit
journal). The `warn` `commit-drift` finding got a STATE §Runtime row update
plus this journal entry — no GitHub issue this cycle (1st occurrence, dedup
search returned #69 as a distinct prior episode, not a duplicate to
comment on or reopen against). Drained inbox to zero; inbox file deleted in
this commit.

## Corrections
`HEAD` was detached at session start (local ref stale from container start,
one commit behind at `11fc669`); `git fetch origin main` + `git checkout -B
main origin/main` reset the local branch cleanly before any probing —
nothing was stranded. `gh` CLI is not installed in this session (unlike
some prior audit sessions that apparently had it, per their journals'
literal `gh run list` invocations) — used the equivalent
`mcp__github__actions_list` / `mcp__github__get_job_logs` MCP tools instead,
same field shapes, noted here in case a future cycle hits the same gap.
Issues #71 and #72 (frozen ledger rows) are truth-cycle territory, not
touched here.
