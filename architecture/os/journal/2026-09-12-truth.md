---
kind: journal
cycle: truth
date: 2026-09-12
---

# Truth cycle — 2026-09-12

## Actions

1. `git fetch origin` — local `HEAD` was detached at `c76bec7d` (the
   09-12T07:02Z audit commit) while local `main` was stale 19 commits
   behind. `git checkout main && git pull --ff-only` fast-forwarded to
   `origin/main` (`c76bec7d`) — working tree unaffected, no functional
   impact.
2. `pnpm install --frozen-lockfile` — clean. Container ships Node 22 by
   default (`/opt/node22`); prepended `/opt/node20/bin` (v20.20.2, matches
   the repo's pinned `20.18.1` major) to `PATH` for install/build/test so
   the CommonJS-on-Node-20 constraint (`OS.md` NEVER #6) is actually
   exercised.
3. `pnpm --filter @keyflow/db db:generate` — fresh clone had no
   `node_modules/.prisma`; generated in ~15s per truth.md's standing
   warning about the `--filter build` bypass of turbo's dependency graph.
4. Build: `@keyflow/shared` + `@keyflow/db` + `@keyflow/api` (tsc, all
   green), then `server` (tsc, green).
5. Gate suite: `cd apps/server && pnpm test:unit` (`vitest -c
   vitest.unit.config.ts`) — **373 files / 3,658 tests passed, 0 skipped.**
   Seed `1789197228567` (`--sequence.seed=1789197228567` replays it). Did
   NOT run the bare `npx vitest run` (would pull in the DB/Redis-dependent
   `test/**` suites and false-red as skips per issue #66).
6. Regenerated all four families: `generate-registries.js` (5 files, only
   `generated:` date line moved — confirmed via `git diff`), capability-map
   `generate.js` (byte-identical output to 2026-09-11: 286 flowTools, 207
   cortexCapabilities, 12 domains, 32 targets), `inventory.py` (+5
   files/+499 lines, fully attributed below), `dependency_scan.py`
   (byte-identical to 2026-09-11, no drift).
7. Re-derived every `STATE.md` §Shape row — all 12 counting commands plus
   the two supplementary test-count commands (`apps/web` vitest: 210/22
   files; `packages/{db,api}`: 12/2 files, db 7 + api 5) matched their
   `Prev` values exactly (no change). `Routes mapped at boot` stays `—` (no
   docker in this sandbox).
8. `node scripts/os/ledger-sizes.mjs` vs `Prev` — all 14 ledgers checked;
   none moved wrong-direction (see Deltas). No new `gate-integrity` issue
   opened.
9. Gate-file integrity: `git diff --name-only d91b935b..HEAD | grep -E
   '\.(spec|test)\.ts$'` → empty. Zero spec/test files touched since the
   last truth commit (`d91b935b`, 2026-09-11), so nothing to intersect with
   the ledgered gate files.
10. DOC_DEBT sweep: reran both rows' disproving commands.
    - Row 1 (`architecture-risks.md` R3/R21): schema scan finds 221
      `businessId String` model declarations and `BUSINESS_ID_MODELS`
      (`packages/db/src/client.ts`) has 326 entries — the doc's R3/R21 body
      text (lines 50, 68) still asserts the stale "77 of 337 (~23%)"
      figures; only the 2026-08-11 warning banner at the top corrects it.
      Claim is still false. Not a candidate for removal.
    - Row 2 (`docs/CAPABILITY_MAP_2026-08-09.md`): still headlines "245"
      tools and "439" models; actual is 286 tools
      (`count-flow-tools.mjs`) / 441 models (schema grep). No superseded
      banner present. Claim is still false. Not a candidate for removal.
    - No new provably-false doc claim surfaced.
11. Checked issues #71 (`tenant.never_scope`, frozen 3 vs. real 10) and #72
    (`auth.public_handlers`, frozen 219 vs. real 223) via `issue_read` —
    both still **open**, no new comments or activity since 2026-08-31.
    Displayed STATE.md values for both rows stayed frozen at their prior
    Value/Prev (3/3, 219/219) per truth.md step 6 — this is not a new
    movement, the underlying real counts (10, 223) are unchanged from
    2026-09-11's reading.
12. Updated `STATE.md`: `derived:` → 2026-09-12, all §Shape/§Ledger
    Prev-columns rolled to today's (unchanged) values, seed updated, and
    the §Generated artifacts freshness narrative rewritten with today's
    inventory delta and commit references.
13. Staged only the intended paths and verified with `git diff --cached
    --name-only` before committing (see Deltas for the exact list).

## Deltas

- **Registries** (`architecture/{module,route,event,capability,
  data-ownership}-registry.yaml`): only the `generated:` date line changed
  on all five files — zero structural drift.
- **Capability map** (`docs/architecture/capability-map/*`,
  `apps/server/.../capability-map.seed.ts`): byte-identical to 2026-09-11.
  No commit needed for these paths (nothing to add).
- **`architecture/inventory.json`**: `total_files` 4323→4328 (+5),
  `total_lines` 1,043,270→1,043,769 (+499). Fully attributed to 5 new
  journal files committed since `d91b935b`: `2026-09-11-audit-13.md`
  (125 lines), `2026-09-11-audit-19.md` (77), `2026-09-11-truth.md` (119,
  the prior cycle's own journal), `2026-09-12-audit-01.md` (94),
  `2026-09-12-audit-07.md` (84) = 499 lines / 5 files exactly. The
  `.claude/coordination/sessions/<uuid>.json` filename swap is this
  session's own coordination file replacing the prior session's (net-zero,
  same shape). `STATE.md`'s own recorded size moved 9,289→9,800 bytes,
  reflecting the audit cycle's §Runtime-only writes since `d91b935b` —
  expected under the write matrix, not truth-cycle drift. No unexplained
  movement.
- **`architecture/dependencies.json`**: byte-identical to the committed
  2026-09-11 file.
- **Ledger cardinalities** (`node scripts/os/ledger-sizes.mjs`): all 14
  rows re-derived; 12 matched Prev exactly, 2 (`tenant.never_scope`,
  `auth.public_handlers`) remain frozen per open issues #71/#72 with their
  underlying real values unchanged (10 and 223 respectively) from
  2026-09-11 — no new divergence, no new issue filed.
- **Tests**: server unit 3,658/373/0-skip (seed `1789197228567`), web
  210/22, packages db 7 + api 5 = 12/2 — all identical counts to
  2026-09-11.

## Corrections

- None. The run followed the playbook exactly on the first pass; the only
  wrinkle (detached `HEAD` at session start) is the same benign condition
  every recent truth cycle has hit when a same-day audit commit lands
  before the nightly truth cycle checks out `main`, and resolves with the
  standard `git checkout main && git pull --ff-only`.
