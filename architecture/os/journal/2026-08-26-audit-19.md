---
kind: journal
cycle: audit
run: 2026-08-26T19:02Z
---

# Audit cycle — 2026-08-26 19:02Z

## Path taken
Direct probe attempted first: `curl https://keyflowos.com/api/healthz` →
`curl: (56) CONNECT tunnel failed, response 403`. `$HTTPS_PROXY/__agentproxy/status`
showed no successful CONNECT to `keyflowos.com` in its recent-relay-failures
window (only unrelated `mcp.supabase.com` 403s logged). Direct egress remains
blocked — 13th consecutive run on the CI fallback. Fell back to playbook
step 1b.

## Findings
- **prod.health.via-ci** (`info`) — most recent completed `uptime-monitor.yml`
  run 33002201475 succeeded at 2026-08-26T18:53:42Z, ~9 min before this
  audit. Prod confirmed healthy indirectly.
- **commit-drift** (`warn`) — no new `deploy-drift.yml` run fired since the
  last audit (13:33Z): the most recent completed run is still 32946923369
  (2026-08-26T08:16:38Z, `failure`), the same run already recorded in
  STATE.md. Numbers are unchanged: prod (`ea9a21fc6dca`) is 70 commits /
  ~14 days behind main. This is the 6th consecutive audit cycle seeing this
  condition. Issue #69 is already open and correctly scoped to this check;
  no new issue filed and no comment added (STATE.md carries current numbers;
  the next `deploy-drift.yml` run is expected around 2026-08-27T08:00Z given
  its daily schedule).
- **route-parity.skipped-no-egress** (`info`) — CI-fallback path taken, so
  the route-parity oracle step is skipped (no CI substitute), not re-filed
  as `probe.oracle-blind`.
- **diagnostics.unprobed** (`info`) — no `ADMIN_BEARER` configured.
- **queue.business-events.unprobed** (`info`) — direct egress blocked, CI
  fallback exposes no queue metrics.

## Actions taken
- Inbox file `audit-cycle-2026-08-26.jsonl` written with the 5 findings
  above, triaged, and deleted in this commit (0 fail, 1 warn, 4 info).
- STATE.md §Runtime rows updated with this run's results and timestamp
  (2026-08-26T19:02Z).
- No new GitHub issue opened (the one `warn` is already tracked by #69).

## Corrections
None — this cycle matched the expected pattern exactly: no new
`deploy-drift.yml` run had fired yet (its previous run was earlier today,
08:16Z; the next is a daily cadence so wasn't due), and the uptime-monitor
CI fallback confirmed prod health as in every recent cycle. Also noted that
the local checkout's HEAD started this run detached at the same commit as
`origin/main` (05156d4) — re-pointed to a tracking `main` branch before
making any changes; no divergence existed, so no data was at risk.
