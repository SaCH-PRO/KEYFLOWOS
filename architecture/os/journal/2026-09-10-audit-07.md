---
kind: journal
cycle: audit
ran: 2026-09-10T07:01Z
---

# Audit cycle — 2026-09-10 07:01Z

## Step 1: prod health

### 1a — direct probe
`curl -sS -m 15 https://keyflowos.com/api/healthz` → `curl: (56) CONNECT
tunnel failed, response 403`. `$HTTPS_PROXY/__agentproxy/status`
`recentRelayFailures` shows only `mcp.supabase.com:443` (403) this run, but
the direct CONNECT attempt itself confirms `keyflowos.com:443` is still
blocked — 70th consecutive audit citing this gap (unchanged since
2026-08-24T01:02Z). Fell back to 1b.

### 1b — CI fallback
`gh` is still not installed in this sandbox; used `mcp__github__actions_list`
(same field shape as `gh run list`). Most recent `status:completed` run of
`uptime-monitor.yml`: `34447683663`, `conclusion:success`, created
`2026-09-10T06:58:39Z` — ~3min before this cycle started (07:01:41Z), well
inside the 30-min freshness window → `info` `prod.health.via-ci`. That run's
`head_sha` is `27d525c4134f5785424315ae742168077de08dbb`, which is an exact
match for `origin/main` HEAD (`git fetch origin main` confirmed the same
SHA) — prod healthy, confirmed indirectly, against the current head exactly.

Commit-drift via CI: most recent `status:completed` run of `deploy-drift.yml`:
`34328157919`, `conclusion:success`, created `2026-09-09T08:15:49Z`,
head_sha `96e05439` — ~22h46m old now, still inside the 26h staleness window
but the margin has narrowed since the 01:01Z cycle (was ~16h46m old then, no
newer run has fired since). Next daily run expected ~2026-09-10T08:1xZ,
before the 13:01Z cycle. → `info` `commit-drift`, prod current, zero drift.

## Step 2: route parity
CI-fallback path taken in step 1 → route parity has no CI substitute (the
oracle must hit prod's own routes) → skipped entirely, per playbook.
Journaled `info` `route-parity.skipped-no-egress`, not re-filed as
`probe.oracle-blind`.

## Diagnostics / queue
No `ADMIN_BEARER` secret configured (`env | grep -i ADMIN_BEARER` — no
match) → `info` `diagnostics.unprobed`. Direct egress to
`/api/healthz/events` is also blocked, so business-events queue depth has no
signal via either path this cycle → `info`
`queue.business-events.unavailable`.

## Steps 3-5: inbox
Wrote 5 `info` lines to
`architecture/os/inbox/audit-cycle-2026-09-10.jsonl` (`prod.health.via-ci`,
`commit-drift`, `route-parity.skipped-no-egress`, `diagnostics.unprobed`,
`queue.business-events.unavailable`). No other producer files were present
in the inbox at start (only `README.md`). No `warn`/`fail` findings this
cycle — nothing to escalate, so no dedup search against open
`runtime-finding` issues was needed.

Triage: all 5 `info` findings are journal-only per the triage table, with
current values also reflected in the corresponding `STATE.md` §Runtime rows
(unconditional per-cycle tracking, consistent with every prior audit
journal). Drained inbox to zero; inbox file deleted in this commit.

## Corrections
None — clean cycle. Prod healthy via CI, exact head-SHA match on the
uptime-monitor run (stronger confirmation than most cycles, which only see a
recent-enough SHA). Commit-drift confirmed fresh and zero, though its
supporting run is now noticeably older (~22h46m, was ~16h46m six hours ago)
— still well inside the 26h window, but worth a downstream note if the next
scheduled `deploy-drift.yml` run (expected ~08:1xZ) doesn't land before the
13:01Z cycle, which would push it past freshness. Direct-egress gap
unchanged and already tracked (not re-filed). Repo checkout started detached
from `main`; `git fetch origin main` confirmed `origin/main` at
`27d525c4...`, matching this cycle's cited uptime-monitor head_sha exactly —
no divergence to reconcile.
