---
kind: journal
cycle: audit
run: 2026-08-26T13:33Z
---

# Audit cycle — 2026-08-26 13:33Z

## Path taken
Direct probe attempted first: `curl https://keyflowos.com/api/healthz` →
`curl: (56) CONNECT tunnel failed, response 403`, confirmed via
`$HTTPS_PROXY/__agentproxy/status` (no successful CONNECT to keyflowos.com
in the recent-relay-failures window; the only entries were unrelated
`mcp.supabase.com` 403s). Direct egress remains blocked — 12th consecutive
run on the CI fallback. Fell back to playbook step 1b.

## Findings
- **prod.health.via-ci** (`info`) — most recent completed `uptime-monitor.yml`
  run 32974323079 succeeded at 2026-08-26T13:27:37Z, ~6 min before this
  audit. Prod confirmed healthy indirectly.
- **commit-drift** (`warn`) — a *new* `deploy-drift.yml` run fired since the
  last audit: run 32946923369, completed 2026-08-26T08:16:38Z, `failure`.
  The prior audit (07:04Z) had only seen run 32825752514 (2026-08-25T08:16Z)
  and expected the next daily run around 2026-08-26T08:00Z — that expectation
  was correct, and the new run confirms drift has *widened*: prod
  (`ea9a21fc6dca`) is now 70 commits / ~14 days behind main (previously 64 /
  ~13d), oldest missing commit still 2026-08-12T00:38:24Z. This is the 5th
  consecutive audit cycle seeing this condition. Issue #69 is already open
  and scoped to this check; no new issue filed, and no comment added to #69
  this cycle (STATE.md + journal carry the updated numbers; commenting on
  every 6-hourly recurrence would be noise — a human tracking #69 can read
  the linked STATE.md row for the current drift size).
- **route-parity.skipped-no-egress** (`info`) — CI-fallback path taken, so
  the route-parity oracle step is skipped (no CI substitute), not re-filed
  as `probe.oracle-blind`.
- **diagnostics.unprobed** (`info`) — no `ADMIN_BEARER` configured.
- **queue.business-events.unprobed** (`info`) — direct egress blocked, CI
  fallback exposes no queue metrics.

## Actions taken
- Inbox file `audit-cycle-2026-08-26.jsonl` written with the 5 findings
  above, triaged, and deleted in this commit (0 fail, 1 warn, 4 info).
- STATE.md §Runtime rows updated with this run's results and timestamp
  (2026-08-26T13:33Z).
- No new GitHub issue opened (the one `warn` is already tracked by #69).

## Corrections
None — the prior audit's expectation ("next drift run expected
~2026-08-26T08:00Z") was correct, and it fired as run 32946923369, closing
that open question. Nothing else in this run surprised me relative to the
last several cycles' pattern.
