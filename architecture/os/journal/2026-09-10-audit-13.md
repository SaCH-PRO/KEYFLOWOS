---
kind: journal
cycle: audit
ran: 2026-09-10T13:02Z
---

# Audit cycle — 2026-09-10 13:02Z

## Step 1: prod health

### 1a — direct probe
`curl -sS -m 15 https://keyflowos.com/api/healthz` → `curl: (56) CONNECT
tunnel failed, response 403`. Same for `/api/readyz`. `$HTTPS_PROXY/__agentproxy/status`
`recentRelayFailures` shows only `mcp.supabase.com:443` (403) noted separately,
but the direct CONNECT attempts to `keyflowos.com:443` themselves came back
`connect_rejected` — 71st consecutive audit citing this gap (unchanged since
2026-08-24T01:02Z). Fell back to 1b.

### 1b — CI fallback
`gh` is not installed in this sandbox; used `mcp__github__actions_list`
(same field shape as `gh run list`). Most recent `status:completed` run of
`uptime-monitor.yml`: `34479694821`, `conclusion:success`, created
`2026-09-10T12:57:29Z` — ~5m30s before this cycle started (13:02:52Z), well
inside the 30-min freshness window → `info` `prod.health.via-ci`. That run's
`head_sha` is `d3185b041d7904102953fd51e29f7ce45549028c`, an exact match for
`origin/main` HEAD (`git fetch origin main` confirmed the same SHA) — prod
healthy, confirmed indirectly, against the current head exactly.

Commit-drift via CI: most recent `status:completed` run of `deploy-drift.yml`:
`34454222931`, `conclusion:success`, created `2026-09-10T08:15:53Z`,
head_sha `d3185b04` — also an exact match for `origin/main` HEAD — ~4h47m
old, comfortably inside the 26h staleness window (this is the run the 07:01Z
cycle was anticipating; it landed at 08:15Z as expected, well before this
cycle). → `info` `commit-drift`, prod current, zero drift.

## Step 2: route parity
CI-fallback path taken in step 1 → route parity has no CI substitute (the
oracle must hit prod's own routes) → skipped entirely, per playbook.
Journaled `info` `route-parity.skipped-no-egress`, not re-filed as
`probe.oracle-blind`.

## Diagnostics / queue
No `ADMIN_BEARER` secret configured (`env | grep -i ADMIN_BEARER` — no
match) → `info` `diagnostics.unprobed`. Direct egress to
`/api/healthz/events` is also blocked, so business-events queue depth has no
signal via either path this cycle → `info`
`queue.business-events.unavailable`.

## Steps 3-5: inbox
Wrote 5 `info` lines to
`architecture/os/inbox/audit-cycle-2026-09-10.jsonl` (`prod.health.via-ci`,
`commit-drift`, `route-parity.skipped-no-egress`, `diagnostics.unprobed`,
`queue.business-events.unavailable`). No other producer files were present
in the inbox at start (only `README.md`). No `warn`/`fail` findings this
cycle — nothing to escalate, so no dedup search against open
`runtime-finding` issues was needed.

Triage: all 5 `info` findings are journal-only per the triage table, with
current values also reflected in the corresponding `STATE.md` §Runtime rows
(unconditional per-cycle tracking, consistent with every prior audit
journal). Drained inbox to zero; inbox file deleted in this commit.

## Corrections
None — clean cycle, and a slightly stronger one than the 07:01Z run: both
the uptime-monitor run and the deploy-drift run now cite `head_sha`
`d3185b04`, an exact match to current `origin/main` HEAD on *both* checks
(the 07:01Z cycle's deploy-drift evidence was still on the prior head
`96e05439`, since superseded). Direct-egress gap unchanged and already
tracked (not re-filed). Repo checkout started detached at `origin/main`
(`d3185b0`); reset local `main` to track it cleanly before probing — no
divergence to reconcile.
