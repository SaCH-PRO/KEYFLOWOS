---
kind: journal
cycle: audit
at: 2026-09-03T13:02Z
---

# Audit cycle — 2026-09-03T13:02Z

## Path taken

Direct probe attempted first, per playbook: `curl -m 15 https://keyflowos.com/api/healthz`
failed with `CONNECT tunnel failed, response 403`, confirmed via
`$HTTPS_PROXY/__agentproxy/status` as `connect_rejected` (org policy). Fell
back to CI path (step 1b). This is the 43rd consecutive audit with direct
egress blocked — the allowlist fix noted in the playbook (§ Production
reachability, path 1) has not landed yet.

## Findings

- **prod.health.via-ci** (`info`): most recent completed `uptime-monitor.yml`
  run, 33758256659, succeeded at 2026-09-03T12:57:36Z — ~5 minutes before
  this audit ran. Well inside the 30-minute freshness window. Prod healthy.
- **commit-drift** (`info`): most recent completed `deploy-drift.yml` run is
  now 33732471659 (2026-09-03T08:16:29Z, `success`) — a *new* run since the
  last audit (07:02Z), which had cited the older 33607833269
  (2026-09-02T08:16:23Z). ~4h46m old, within the 26h staleness window. Prod
  current/within threshold. Issue #69 (opened 2026-08-25 for a real
  13-day/64-commit drift) remains open awaiting human close — that's
  historical, unrelated to this cycle's clean result.
- **route-parity.skipped-no-egress** (`info`): CI-fallback path has no
  route-parity substitute (the oracle needs direct egress to hit prod's
  actual routes). Skipped per playbook step 2; not re-filed as
  `probe.oracle-blind` (that's reserved for the direct path misbehaving,
  not a known egress gap — issue #65 already covers the historical case).
- **diagnostics.unprobed** (`info`): no `ADMIN_BEARER` secret configured;
  also moot this run since direct egress is down anyway.
- **queue.business-events.unprobed** (`info`): `/api/healthz/events` not
  reachable via CI fallback — no queue-depth signal this cycle, same gap as
  every audit on this path.

No `warn`/`fail` findings — nothing escalated, no new GitHub issue opened.
Existing open issues (#64 prod.unreachable, #65 probe.oracle-blind, #69
commit-drift, #70 uptime-monitor.stale) are historical records from past
incidents/egress-gap discovery and remain open for human triage; none of
them re-fired this cycle.

## Inbox

Wrote `architecture/os/inbox/audit-cycle-2026-09-03.jsonl` (5 `info` lines),
drained it in full this same commit (all `info` → journal only, per
`inbox/README.md` §Triage). No files left over from other producers.

## Corrections

None. The initial `git checkout main` (attempting to leave a detached HEAD
from a prior session) landed on a **stale local `main`** that was 22 commits
behind `origin/main` — this silently reverted `STATE.md` and the journal
directory to their 2026-08-30 state in the working tree before any edits
were made. Caught it before editing further by noticing the re-read
`STATE.md` didn't match what had been read at the start of this run;
`git pull origin main` fast-forwarded local `main` to `origin/main`
(b3f410c) and restored the correct file state, with the untracked inbox
file preserved throughout. No STATE.md or journal content was actually lost
or overwritten on origin — the revert was local-working-tree only, never
committed. Lesson for future audit cycles: prefer `git pull origin main`
directly over `git checkout main` + `git status` when starting from a
detached HEAD, since a local branch ref can be stale relative to origin.
