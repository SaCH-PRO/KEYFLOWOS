# Architecture Risks

> **VERIFIED AGAINST THE CODE 2026-08-11 — R3 and R21 below are STALE, and R21's
> remediation advice is now actively dangerous. Read this before acting on either.**
>
> Measured, re-derivable with `grep -c '^model ' packages/db/prisma/schema.prisma`
> and by parsing `BUSINESS_ID_MODELS` out of `packages/db/src/client.ts`:
>
> | | doc says | actual 2026-08-11 |
> |---|---|---|
> | models with `businessId` | 337 | **348** |
> | scoped by the extension | 77 (~23%) | **303 (87%)** |
> | unlisted / backlog | ~260 | **42 acknowledged + 3 never-scope** |
> | Prisma models total | ~439 | **440** |
>
> The extension also now intercepts **fifteen** operations, not ten: `create`,
> `createMany`, `upsert`, `aggregate` and `groupBy` were added, which is the
> specific omission R3 describes.
>
> **DO NOT scope `Payment` or `MarketplaceOrder`, which both rows list as
> backlog.** They are resolved by a GLOBAL provider key in
> `handleStripeWebhook` / `handlePaypalWebhook`, which derive the business FROM
> the row because a webhook cannot know it beforehand. Prisma 6.19 accepts an
> extra scalar in a `WhereUniqueInput` rather than rejecting it — measured, with
> a negative control — so scoping them would turn a taken payment into a silent
> `null`: no error, no log, no provider retry. They are in a deliberate
> `NEVER_SCOPE` set with that reasoning recorded in `client.ts`.
>
> Of the models these rows name as missing, seven are now scoped:
> `KeyActionProposal`, `EventAttendee`, `TemporalFlowMemory`,
> `SubscriptionPayment`, `Webhook`, `ActivityLog`, and `Task`. Still genuinely
> unscoped and on the acknowledged ledger: `Membership`, `ApiKey`,
> `SocialConnection` — each for a stated reason (`Membership.userId` lookups are
> business-agnostic by design; the others resolve by external key).
>
> The live ledger is `ACKNOWLEDGED_UNSCOPED` in
> `apps/server/src/core/prisma/tenant-model-list.spec.ts`, which is gated to
> shrink only. Trust that file over this one.



This document records critical, high, medium, and low risks identified during baseline cartography and the August 2026 security/tenant/privacy audit. It should be reviewed and updated after significant changes.

## Risk Registry

| ID | Severity | Category | Risk | Evidence | Recommended Next Step |
|----|----------|----------|------|----------|-----------------------|
| R1 | Critical | Coupling | Heavy `forwardRef` cycles between AI/Cortex/Autonomy/Commerce/CRM create brittle, hard-to-test modules. | `apps/server/src/modules/ai`, `key-cortex`, `key-autonomy`, `commerce`, `crm` import each other via `forwardRef`. | Introduce domain event commands and an anti-corruption layer; extract shared DTOs/contracts. |
| R2 | Critical | Scale | `key-cortex` is the largest module (~248 files, ~80 services) and a single point of change amplification. | Module registry shows `key-cortex` has very high fan-in and fan-out. | Split into `key-context`, `key-reasoning`, `key-insight`, `key-agent` sub-modules with explicit public interfaces. |
| R3 | High | Data / Tenant Isolation | Tenant isolation Prisma extension does not intercept `create`, `createMany`, `upsert`, `aggregate`, `groupBy`, and only 77 of 337 `businessId`-bearing models are listed in `BUSINESS_ID_MODELS`. | `packages/db/src/client.ts:81-153` — `BUSINESS_ID_MODELS` contains 77 entries.<br>`packages/db/src/client.ts:234-278` — extension hooks only ten operations and explicitly omits writes.<br>Schema scan: 337 models define `businessId String` (`packages/db/prisma/schema.prisma`). | Extend the extension to reject/rewrite `businessId` on `create`/`createMany`/`upsert` or add a CI lint/spec that enforces `businessId` from validated params; generate `BUSINESS_ID_MODELS` from the schema and backfill high-risk tables. |
| R4 | High | Data / Tenant Isolation | Cron, BullMQ workers, WebSocket handlers, and webhooks run without tenant context; WebSocket approval/execution and TemporalFlowMemory updates are cross-tenant. | `apps/server/src/core/tenant/tenant.interceptor.ts:13-31` — HTTP-only interceptor.<br>`apps/server/src/modules/ai/queue.service.ts:60-87` — BullMQ workers without tenant context.<br>`apps/server/src/modules/key-cortex/key-cortex.gateway.ts:232-347` — WebSocket handlers without tenant context; lines 308-347 approve/reject proposals without passing `clientInfo.businessId`.<br>`packages/db/src/client.ts:121-127` — comment documenting unprotected paths.<br>`apps/server/src/modules/temporal-flow/temporal-flow-memory.service.ts:130-150` — updates memory by id only. | Provide `runWithTenant(businessId, systemUserId, …)` wrapper for background/edge handlers; pass and validate `clientInfo.businessId` in every WebSocket message handler; add `KeyActionProposal` and `TemporalFlowMemory` to `BUSINESS_ID_MODELS`. |
| R5 | High | Security | Token encryption fallback key is hardcoded for local dev. | `packages/db/src/middleware/token-encryption.ts` fallback chain. | Block production startup if only fallback key is present. |
| R6 | High | Reliability | `Payment` and `MarketplaceOrder` are excluded from `BUSINESS_ID_MODELS` because webhooks resolve business from provider keys. | Code comments and exclusion list. | Add explicit tests for webhook-to-business resolution; never scope these models by inferred `businessId`. |
| R7 | Medium | Maintenance | Legacy monolithic API client `apps/web/src/lib/client.ts` coexists with newer `src/lib/api/*` domain wrappers. | Web inventory lists both. | Gradually migrate consumers to domain-specific wrappers; deprecate `client.ts` usages. |
| R8 | Medium | Operations | 224 `/app/**` routes exist but only 67 are in nav; 157 are potentially orphaned or discoverable only by direct URL. | `architecture/route-registry.yaml`. | Review orphaned routes; remove dead pages or add launcher/nav entries. |
| R9 | Medium | Operations | Event registry shows 111 published-only and 30 listened-only events, indicating dead or incomplete event contracts. | `architecture/event-registry.yaml`. | Audit events; remove dead ones or add listeners; document intentional publish-only events. |
| R10 | Medium | Coupling | `modules/ai` is imported by 30+ modules, making AI provider changes high-impact. | `architecture/module-registry.yaml` `registeredVia` for `ai`. | Stabilize the `ModelGatewayService` public interface; avoid leaking internal provider types. |
| R11 | Medium | Data | Prisma schema has ~439 models with many legacy/experimental tables (e.g., `Task`, `Message`, `Campaign` at end of file). | `packages/db` cartography notes. | Identify unused models; plan deprecation or rename to `Legacy*` to signal status. |
| R12 | Medium | Build | Server must be compiled with `tsc`; `tsx` breaks NestJS decorator metadata. | `AGENTS.md` launch notes. | Keep build in dev loop; consider build watch (`tsc --watch`) to improve feedback. |
| R13 | Low | Tooling | `packages/api/src/context.ts` references tRPC v11 docs but project uses tRPC v10; file appears unused. | Phase 1 packages report. | Confirm it is dead code and remove. |
| R14 | Low | Completeness | `adminRouter` and `automationRouter` contain stub/demo data; real implementations live elsewhere. | Phase 1 packages report. | Decide whether to implement or redirect to server modules. |
| R15 | Low | Frontend | Next.js 16 + Turbopack cold-start crash requires `--webpack` dev flag. | `AGENTS.md` launch notes. | Track upstream Next.js fix; remove patch/flag when stable. |
| R16 | Critical | Cryptography / Data Leak | OAuth, bank, social, and channel access/refresh tokens are stored in plaintext at rest. | `packages/db/prisma/schema.prisma:506-565` — `Business` token columns (gmail*, calendar*, drive*, forms*, contacts*, msContacts*, bp*).<br>`packages/db/prisma/schema.prisma:3823-3824` — `SocialConnection.token` / `refreshToken` plaintext.<br>`packages/db/prisma/schema.prisma:3874-3875` — `ChannelConnection.token` / `refreshToken` plaintext.<br>`packages/db/prisma/schema.prisma:10061` — `BankConnection.accessToken` plaintext.<br>`apps/server/src/core/connectors/google-suite.service.ts:193-196` writes tokens directly to `Business` columns.<br>`apps/server/src/modules/connect/microsoft-oauth.service.ts:108-116` writes tokens directly to `Business` columns.<br>`apps/server/src/modules/social/social-connections.service.ts:61-113` writes `token` / `refreshToken` directly.<br>`apps/server/src/modules/finance/bank-connection.service.ts:68-69` writes `accessToken` directly. | Encrypt all token columns using existing AES-256-GCM utilities (`token-crypto.ts`, `ConnectorCredentialsService`); backfill existing plaintext rows; decrypt only at the service boundary. |
| R17 | Critical | Session Management | Supabase JWT local verification never checks revocation or user state. | `apps/server/src/core/auth/supabase-auth.service.ts:101-120` returns user from `verifyLocal` without calling Supabase.<br>`apps/server/src/core/auth/auth.middleware.ts:88-104` only looks up `role`, no status/banned check.<br>`packages/db/prisma/schema.prisma:101-130` — `User` model has no `status` / `bannedAt` / `deletedAt`. | **MITIGATED 2026-08-09:** Added `deletedAt`/`bannedAt` columns to `User`; `AuthMiddleware` rejects tokens for deleted/banned/revoked users; `SupabaseAuthService` rejects `banned_at` users in the fallback round-trip; Redis revocation cache populated by `POST /identity/logout`. Remaining: RISC/password-change should also populate the revocation cache. |
| R18 | Critical | Authentication / Audit | Password reset and change flows are handled client-side against Supabase, bypassing backend rate limits, audit, and password policy. | `apps/web/src/app/auth/login/login-form.tsx:33-34, 53-64` calls `${SUPABASE_URL}/auth/v1/recover` directly using `NEXT_PUBLIC_SUPABASE_ANON_KEY`.<br>`apps/web/src/app/auth/reset-password/page.tsx:13-17` enforces policy client-side only.<br>`apps/web/src/app/app/profile/components/security-section.tsx:79-105` calls `/auth/v1/user` directly to change password.<br>`apps/server/src/modules/identity/password-policy.service.ts` is only invoked during signup. | Add server endpoints `/identity/forgot-password`, `/identity/reset-password`, and `/identity/me/password` that apply `AuthSecurityService` rate limits, write `AuthAuditLog`, and run `PasswordPolicyService.validate`. |
| R19 | Critical | Authorization / Middleware Bypass | Admin console routes (`/admin/*`) have no server-side gate; protection is client-side only. | `apps/web/src/middleware.ts:143-145` matcher only `/app/:path*`.<br>`apps/web/src/app/admin/layout.tsx:40-62` performs auth check in client-side `useEffect` against `localStorage`. | Extend middleware to gate `/admin/:path*` and add a server-side admin auth check to `admin/layout.tsx`. |
| R20 | Critical | Tenant Isolation / Authorization | KEY Cortex WebSocket `key:approve` / `key:reject` handlers can approve/execute another tenant's action proposals. | `apps/server/src/modules/key-cortex/key-cortex.gateway.ts:308-347` `handleApprove`/`handleReject` do not pass `clientInfo.businessId`.<br>`apps/server/src/modules/key-cortex/key-cortex-approval.service.ts:81-98` loads proposal via unscoped `getById`.<br>`apps/server/src/modules/key-autonomy/key-action-proposal.service.ts:110-116` `getById` uses `findUnique({ where: { id } })`.<br>`packages/db/src/client.ts:81-153` — `KeyActionProposal` is not in `BUSINESS_ID_MODELS`. | Pass and validate `clientInfo.businessId` against the proposal before mutating; add `KeyActionProposal` to `BUSINESS_ID_MODELS`; remove/scope the unscoped `getById` production path. |
| R21 | Critical | Tenant Isolation / Misconfiguration | Tenant isolation extension covers only ~23% of business-scoped tables. | `packages/db/src/client.ts:81-153` — `BUSINESS_ID_MODELS` contains 77 entries.<br>Schema scan: 337 models define `businessId String` (`packages/db/prisma/schema.prisma`).<br>Missing examples: `KeyActionProposal`, `Event`, `EventAttendee`, `TemporalFlowMemory`, `Payment`, `MarketplaceOrder`, `SubscriptionPayment`, `Membership`, `ApiKey`, `SocialConnection`, `Webhook`, `Task`, `Message`, `Document`, `Lead`, `Campaign`, `HelpdeskTicket`, `ActivityLog`. | Generate `BUSINESS_ID_MODELS` from the schema (any model with `businessId String` minus explicit exclusions); treat the 260 missing models as a backlog and add high-risk tables first. |
| R22 | Critical | Privacy / Data Deletion | GDPR hard-purge fails for contacts with invoices, quotes, bookings, or recurring invoices. | `apps/server/src/modules/crm/privacy/contact-privacy.service.ts:448-523` counts child records but only calls `tx.contact.delete()` at line 507.<br>Migration FKs `ON DELETE NO ACTION`: `quotes.contact_id` (`migration.sql:10481`), `invoices.contact_id` (`migration.sql:10496`), `bookings.contact_id` (`migration.sql:10532`), `recurring_invoices.contact_id` (`migration.sql:10745`). | Delete or anonymize the financial rows before `tx.contact.delete()`; make the operation idempotent and test all four child types. |
| R23 | Critical | Privacy / Audit | GDPR purge leaves full PII snapshots in `BusinessEvent`. | `apps/server/src/modules/business-events/business-event.service.ts:24-60` stores raw JSON snapshots.<br>`apps/server/src/modules/key-autonomy/action-audit.service.ts:81-113` and `apps/server/src/modules/key-cortex/key-cortex-audit.service.ts:26-55` pass raw payloads.<br>`apps/server/src/modules/business-events/business-event.interceptor.ts:94-99` redaction covers only HTTP controller paths. | Extend `purgeOne` to scrub `BusinessEvent` rows whose `subjectType`/`subjectId` or JSON metadata reference the contact, or route all service-side writes through `safeRedactedSnapshot()`. |
| R24 | Critical | Privacy / Data Deletion | `MediaAsset` and `ContactMedia` files survive contact erasure. | `packages/db/prisma/schema.prisma:11191-11215` — `MediaAsset` has `contactId` but no FK/relation.<br>`packages/db/prisma/schema.prisma:3299-3313` — `ContactMedia` relation lacks cascade.<br>`migration.sql:10460` — `contact_media_contact_id_fkey` `ON DELETE SET NULL`.<br>`apps/server/src/modules/crm/privacy/contact-privacy.service.ts:448-523` never touches media rows or S3 objects. | Query `MediaAsset` and `ContactMedia` by `contactId`, delete their S3 objects, then delete the DB rows before deleting the contact. |
| R25 | Critical | Privacy / Data Deletion | SET-NULL child tables retain contact PII after purge. | Migration FKs `ON DELETE SET NULL`: `message_intakes_contact_id_fkey` (`migration.sql:10292`), `ingestion_items_contact_id_fkey` (`migration.sql:10301`), `whatsapp_contacts_contact_id_fkey` (`migration.sql:10583`), `support_tickets_contact_id_fkey` (`migration.sql:10655`), `financial_transactions_contact_id_fkey` (`migration.sql:11384`).<br>`apps/server/src/modules/crm/privacy/contact-privacy.service.ts:448-523` does not scrub these models. | Add a purge step that deletes or anonymizes these rows when the linked contact is forgotten. |
| R26 | High | Authentication / OAuth | Google and Microsoft OAuth flows do not use PKCE. | `apps/server/src/core/connectors/google-suite.service.ts:109-127` builds state/params without PKCE.<br>`apps/server/src/modules/connect/microsoft-oauth.service.ts:43-58` builds state/params without PKCE. | Generate a `code_verifier`, store it server-side keyed by signed state, append `code_challenge_method=S256` and `code_challenge` to the auth URL, and send `code_verifier` during token exchange. |
| R27 | High | Authorization | Keystore "admin" endpoints allow any business member to manage listings and orders. | `apps/server/src/modules/keystore/keystore-admin.controller.ts:28-30` uses only `AuthGuard` + `BusinessGuard`.<br>Admin operation endpoints at lines 35, 42, 56, 64, 79, 94, 111, 119, 128, 144. | Add `ModuleScopeGuard` with `@RequireModuleScope('storefront', 'admin')` or restrict to `OWNER`/`ADMIN` roles. |
| R28 | High | Authorization | API keys can be created and revoked by any business member. | `apps/server/src/modules/api-keys/api-keys.controller.ts:10-34` uses only `AuthGuard` + `BusinessGuard`.<br>`create` accepts `body.scopes ?? []` without validation against caller permissions. | Restrict API key management to `OWNER`/`ADMIN` and/or validate that requested scopes are a subset of the caller's own scopes. |
| R29 | High | Session Management | Regular user logout does not invalidate server-side sessions. | `apps/web/src/hooks/use-app-layout.ts:373-376` `handleLogout` only clears storage and redirects.<br>`apps/web/src/lib/workspace.ts:259-271` clears localStorage/cookies but never calls backend logout. | **MITIGATED 2026-08-09:** Added `POST /identity/logout` (`IdentityController.logout`) which calls `SupabaseAdminService.signOut(userId, 'global')`, sets a 24-hour Redis revocation marker, and is invoked from `use-app-layout.ts:handleLogout`. |
| R30 | High | Session Management | Admin auth has no logout flow and leaves 24-hour cookies behind. | `apps/web/src/app/admin/login/page.tsx:43-50` sets `kf_admin_token`, `kf_admin_user_cache`, `kf_auth_mode` cookies with `max-age=86400`.<br>No callers to `POST /api/admin/auth/logout` found in `apps/web/src`. | Add an admin logout control that calls `POST /api/admin/auth/logout` and clears `kf_admin_token` / `kf_admin_user_cache` / `kf_auth_mode` from both `localStorage` and cookies. |
| R31 | High | Session Management / Cookie Security | Session cookies are non-HttpOnly, JavaScript-accessible, and long-lived. | `apps/web/src/lib/workspace.ts:11, 18-27` writes `kf_token` via `document.cookie` without `HttpOnly`, Max-Age 7 days.<br>`apps/web/src/app/admin/login/page.tsx:48` writes `kf_admin_token` via `document.cookie`.<br>Refresh token stored only in `localStorage`. | Move to HttpOnly, `Secure`, `SameSite=Lax` cookies set by the server; keep a short-lived signed middleware-only cookie for Edge verification; do not store refresh tokens in `localStorage`. |
| R32 | High | Cryptography / Authentication | Local JWT verification ignores standard claims (`iss`, `aud`, `iat`, `nbf`) and header algorithm. | `apps/server/src/core/auth/supabase-auth.service.ts:41-77` verifies only signature and `exp`. | **MITIGATED 2026-08-09:** `SupabaseAuthService.verifyLocal` now validates `alg === 'HS256'`, `iss`, `aud`, `nbf`, and future-`iat`. |
| R33 | High | Tenant Isolation / Data Corruption | `TemporalFlowMemory.updateMemory` can overwrite another tenant's temporal memory. | `apps/server/src/modules/temporal-flow/temporal-flow-memory.service.ts:130-150` reads/updates by id only with no `businessId` check.<br>`apps/server/src/modules/key-cortex/adapters/temporal-adapter.service.ts:58-89` accepts `businessId` but discards it.<br>`packages/db/src/client.ts:81-153` — `TemporalFlowMemory` not in `BUSINESS_ID_MODELS`. | Change `TemporalFlowMemoryService.updateMemory` to accept and enforce `businessId`; add `TemporalFlowMemory` to `BUSINESS_ID_MODELS`. |
| R34 | High | Privacy / Audit | `ContactAuditEntry` retains IP, user agent, and actor email after GDPR purge. | `apps/server/src/modules/crm/privacy/contact-privacy.service.ts:471-480` scrubs `before`/`after`/`actor`/`reason` but not `ip`/`userAgent`.<br>`apps/server/src/modules/crm/privacy/contact-privacy.controller.ts:81-82, 125-127` stores `req.user.email` as actor label. | Set `ip: null, userAgent: null` in the scrub `updateMany`; store only a stable actor user id, never email. |
| R35 | High | Privacy / Access Control | Contact export can access soft-deleted and forget-pending contacts. | `apps/server/src/modules/crm/privacy/contact-privacy.service.ts:539-560` (`collectBundle`) loads contact with `findFirst({ where: { id, businessId } })` and no `deletedAt: null` (line 543).<br>`apps/server/src/modules/crm/privacy/contact-privacy.controller.ts:69-87` requires only `crm:write`. | Reject the export if `contact.deletedAt` is not null; consider redacting audit `before`/`after` in the export bundle. |
| R36 | High | Privacy / Audit | Service-side audit/event writes bypass centralized secret redaction. | `apps/server/src/core/security/redaction.ts:13-76` redaction utility exists.<br>Direct emitters: `apps/server/src/modules/key-autonomy/action-audit.service.ts:89-108`, `apps/server/src/modules/key-cortex/key-cortex-audit.service.ts:26-55`, plus others.<br>`apps/server/src/modules/business-events/business-event.interceptor.ts:30-92` applies redaction only to HTTP controller paths. | Apply `safeRedactedSnapshot()` inside `BusinessEventService.emit()` and `ContactAuditService.write()` so all persisted snapshots pass through one policy. |
| R37 | High | Privacy / Audit | AI execution logs store raw tool payloads without redaction. | `apps/server/src/modules/ai/ai-execution-log.service.ts:82-101` stores tool `args`/`result`.<br>`sanitizePayload()` at lines 169-180 only truncates, never redacts. | Run payloads through `deepRedact()` (or `safeRedactedSnapshot()`) before storing. |
| R38 | High | Privacy / Data Retention | No retention cleanup for export jobs, notification logs, activity logs, or sessions. | `apps/server/src/modules/crm/privacy/contact-privacy.service.ts:188-200` creates `ContactExportJob`; no cleanup job found.<br>`apps/server/src/modules/notifications/transactional-email.service.ts:262-293` creates `customerNotificationLog`; no cleanup.<br>`apps/server/src/core/interceptors/team-audit.interceptor.ts:44` creates `TeamActivityLog`; no cleanup.<br>`apps/server/src/modules/risc/risc.service.ts:318, 371` purges `Session` only on RISC/user deletion. | Add a cron-driven retention worker for each table, with configurable retention windows, and delete orphaned S3 objects. |
| R39 | High | Audit | Security audit's `checkEnvSecrets` is broken and never flags the Supabase service-role key. | `apps/server/src/modules/security-audit/security-audit.service.ts:119-126` ternary always evaluates to `null`. | Correct the ternary to actually evaluate the key (e.g., flag if length < 40 or set in non-production). |
| R40 | High | Secrets in Code | Hardcoded development password and native-AI fallback API key in source. | `apps/server/src/core/seed/seed.service.ts:58` `const DEV_PASSWORD = 'keyflowdev123'`.<br>`apps/server/src/modules/ai/model-gateway.service.ts:1452, 2423` `const apiKey = byokKey \|\| process.env.NATIVE_AI_API_KEY \|\| 'native-key'`. | Generate the dev password randomly at first seed and print it once; remove the `'native-key'` fallback so missing config fails loudly. |
| R41 | High | Authentication / Cryptography | Admin HMAC-JWT uses millisecond `exp` and lacks `iss`/`aud` claims. | `apps/server/src/core/auth/admin-token.util.ts:65-82` stores `exp` as `Date.now() + 24h` in milliseconds and omits `iss`/`aud`. | Issue standard JWTs with `exp` in seconds plus `iss` and `aud` claims, and validate them during verification. |
| R42 | High | Misconfiguration / RISC | RISC receiver endpoint exists but is not registered with Google. | `apps/server/src/modules/risc/risc.controller.ts` and `risc.service.ts` handle ingestion only.<br>No calls to Google's RISC Configuration Admin API found. | Add a one-time or boot-time registration step using the service account to subscribe `RISC_RECEIVER_URL`. |
| R43 | High | Misconfiguration / Reliability | `AuthMiddleware` DI fallback can create a stale, unconfigured auth verifier. | `apps/server/src/core/auth/auth.middleware.ts:37-46` constructs `new SupabaseAuthService()` if NestJS DI does not provide one. | Remove the fallback and let NestJS fail fast at boot if `SupabaseAuthService` cannot be injected; add a dedicated integration test for middleware DI wiring. |
| R44 | Medium | Authentication / OAuth | Social OAuth flows use in-memory sessions and lack PKCE. | `apps/server/src/modules/social/social.controller.ts:351-377` uses 16-byte hex `state` and no PKCE except Twitter.<br>`apps/server/src/modules/social/social-connections.service.ts:14` `private oauthSessions = new Map<...>()`. | Use signed+HMAC state, store state/PKCE in Redis, and add PKCE to all flows. |
| R45 | Medium | Cryptography / OAuth | Google/Microsoft OAuth state nonces are generated with `Math.random()`. | `apps/server/src/core/connectors/google-suite.service.ts:112` `nonce: Math.random().toString(36).slice(2)`.<br>`apps/server/src/modules/connect/microsoft-oauth.service.ts:45` same. | Use `randomBytes(16).toString('hex')` or `randomUUID()` for the nonce. |
| R46 | Medium | Misconfiguration / Rate Limiting | `PublicRateLimitGuard` retains the latent fail-open bug that `RateLimitGuard` fixed. | `apps/server/src/core/guards/public-rate-limit.guard.ts:42` `const [, [, count]] = await pipeline.exec() as [unknown, [null, number]]` without inspecting pipeline errors. | Apply the same error-inspection logic used in `RateLimitGuard` (check each command result; throw if count is not a number). |
| R47 | Medium | Session Management | `fetchWithAuthRetry` does not trigger global logout after a final 401. | `apps/web/src/lib/api.ts:297-315` returns the 401 response without emitting `kf:unauthorized`. | Emit the unauthorized event before returning a still-401 response when refresh fails, matching the typed wrappers. |
| R48 | Medium | Authentication | `AuthMiddleware` swallows invalid bearer tokens and continues as anonymous. | `apps/server/src/core/auth/auth.middleware.ts:60-86` catches verification errors and calls `next()`; lines 81-85 log at debug only. | Set a flag such as `req.authFailed = true` when a token was provided but invalid; log invalid attempts at `warn` with IP and a redacted token fingerprint. |
| R49 | Medium | Tenant Isolation / Input Validation | `TenantInterceptor` trusts client-supplied `businessId` from body/query. | `apps/server/src/core/tenant/tenant.interceptor.ts:16` sources include `req.body`/`req.query`.<br>`apps/server/src/core/auth/business.guard.ts:21` reads same sources but is not applied on every route. | Derive tenant only from URL params in `TenantInterceptor`; remove body/query as sources or require `BusinessGuard` on every business-scoped route via a lint/CI rule. |
| R50 | Medium | Tenant Isolation / Information Disclosure | `KeyCortexApprovalOrchestrator.executeApproved` performs an unscoped read for audit. | `apps/server/src/modules/key-cortex/key-cortex-approval-orchestrator.service.ts:201-241` calls `proposalService.getById(input.proposalId)` before the scoped `execute`. | Use the scoped `proposalService.get(businessId, proposalId)` for the pre-execution snapshot, or remove the unscoped `getById` path from production code. |
| R51 | Medium | Privacy / Access Control | Public contact-export download endpoint lacks rate limiting. | `apps/server/src/modules/crm/privacy/contact-privacy.controller.ts:89-107` download handler has no `@PublicRateLimit`. | Add `@UseGuards(PublicRateLimitGuard)` and `@PublicRateLimit(...)` to the `download` handler. |
| R52 | Medium | Privacy / Consent | `ConsentRecord` lacks unique constraint per contact/channel/type. | `apps/server/src/modules/communications/consent.service.ts:26-50` always inserts a new row.<br>`packages/db/prisma/schema.prisma:11155-11171` no unique index on `(businessId, contactId, channel, consentType)`. | Add a unique constraint and upsert on status change; add an `updatedAt` column. |
| R53 | Medium | Privacy / Consent | Transactional email ignores soft-deleted contacts. | `apps/server/src/modules/notifications/transactional-email.service.ts:327-344` checks `doNotContact` but not `deletedAt`. | Add `deletedAt: null` to the contact lookup in the send gate. |
| R54 | Medium | Privacy / Access Control | Forget request/cancel does not validate contact deletion state. | `apps/server/src/modules/crm/privacy/contact-privacy.service.ts:298-409` `requestForget`/`cancelForget` conflate normal soft-delete with forget flow. | Record deletion source (normal delete vs forget) and only allow cancellation/restoration from the forget flow; reject exports of any `deletedAt != null` contact. |
| R55 | Medium | Privacy / Logging | `AuthMiddleware` writes user email to debug logs. | `apps/server/src/core/auth/auth.middleware.ts:65-76` logs email on successful authentication. | **MITIGATED 2026-08-09:** `AuthMiddleware` now logs only `userId`; email is no longer emitted. |
| R56 | Medium | Privacy / Misconfiguration | Object storage uploads do not enforce private ACL or encryption. | `apps/server/src/core/object-storage/objectStorage.ts:284-288, 307-314` use `PutObjectCommand` without `ACL`/`ServerSideEncryption`.<br>`apps/server/src/modules/crm/privacy/contact-privacy.service.ts:170-186` GDPR export uploads likewise. | Add `ACL: 'private'` and `ServerSideEncryption: 'AES256'` (or KMS) to all `PutObjectCommand` calls; enforce a bucket policy that denies public reads. |

## Risk Severity Definitions

- **Critical** — Could cause production outage, data leak, or block major refactoring without immediate attention.
- **High** — Significant maintenance, security, or correctness concern; should be in the next planning cycle.
- **Medium** — Worth tracking; causes friction but has workarounds.
- **Low** — Cleanup, documentation, or optimization; safe to defer.

## Trending Notes

- The biggest structural risk is still the centralization of AI/Cortex logic. Any plan to split this should remain the top migration priority.
- Tenant isolation gaps are now confirmed with concrete cross-tenant paths (WebSocket approvals, TemporalFlowMemory updates) and a coverage gap of ~77/337 business-scoped models. This is the second-highest priority.
- Plaintext token storage and Supabase revocation bypass are the highest-severity security issues and should be treated as production blockers.
- GDPR hard-purge has four critical completeness bugs that make the erasure flow fail or leave PII behind.
- Dead routes and dead events are low-hanging fruit for cleanup sprints.
