diff --git a/dist/client/components/use-action-queue.js b/dist/client/components/use-action-queue.js
index 6950553c42839a41e87f1d4cb32e7e028f114d13..7822a6919ebf5f1bc51ee50c3598632c7c78f109 100644
--- a/dist/client/components/use-action-queue.js
+++ b/dist/client/components/use-action-queue.js
@@ -106,37 +106,26 @@ function useActionQueue(actionQueue) {
     if (typeof window !== 'undefined') {
         dispatch = nextDispatch;
     }
-    // When navigating to a non-prefetched route, then App Router state will be
-    // blocked until the server responds. We need to transfer the `_debugInfo`
-    // from the underlying Flight response onto the top-level promise that is
-    // passed to React (via `use`) so that the latency is accurately represented
-    // in the React DevTools.
-    const stateWithDebugInfo = (0, _react.useMemo)(()=>{
-        if (!__DEV__) {
-            return state;
-        }
-        if ((0, _isthenable.isThenable)(state)) {
-            // useMemo can't be used to cache a Promise since the memoized value is thrown
-            // away when we suspend. So we use a WeakMap to cache the Promise with debug info.
-            let promiseWithDebugInfo = promisesWithDebugInfo.get(state);
-            if (promiseWithDebugInfo === undefined) {
-                const debugInfo = [];
-                promiseWithDebugInfo = Promise.resolve(state).then((asyncState)=>{
-                    if (asyncState.debugInfo !== null) {
-                        debugInfo.push(...asyncState.debugInfo);
-                    }
-                    return asyncState;
-                });
-                promiseWithDebugInfo._debugInfo = debugInfo;
-                promisesWithDebugInfo.set(state, promiseWithDebugInfo);
-            }
-            return promiseWithDebugInfo;
+    // Work around React 19's `use(thenable)` hook-count bug
+    // (facebook/react#33556, vercel/next.js#78396) by reading any pending
+    // thenable via a tiny useState/useEffect wrapper instead of React's `use`.
+    // This keeps the hook count stable across renders. Suspense fallbacks are
+    // slightly less immediate during navigation, but the app no longer crashes
+    // with "Rendered more hooks than during the previous render".
+    return useUnwrapState(state);
+}
+function useUnwrapState(_state) {
+    const [state, setState] = _react.default.useState(_state);
+    _react.default.useEffect(() => {
+        if ((0, _isthenable.isThenable)(_state)) {
+            _state.then(setState);
+        } else {
+            setState(_state);
         }
-        return state;
     }, [
-        state
+        _state
     ]);
-    return (0, _isthenable.isThenable)(stateWithDebugInfo) ? (0, _react.use)(stateWithDebugInfo) : stateWithDebugInfo;
+    return state;
 }
 
 if ((typeof exports.default === 'function' || (typeof exports.default === 'object' && exports.default !== null)) && typeof exports.default.__esModule === 'undefined') {
diff --git a/dist/esm/client/components/use-action-queue.js b/dist/esm/client/components/use-action-queue.js
index 05ce1854311437bafb4b25b6dbe0b9ea967757a5..d1a8e804910e8be7949ece2ecff9b67680149e11 100644
--- a/dist/esm/client/components/use-action-queue.js
+++ b/dist/esm/client/components/use-action-queue.js
@@ -80,37 +80,26 @@ export function useActionQueue(actionQueue) {
     if (typeof window !== 'undefined') {
         dispatch = nextDispatch;
     }
-    // When navigating to a non-prefetched route, then App Router state will be
-    // blocked until the server responds. We need to transfer the `_debugInfo`
-    // from the underlying Flight response onto the top-level promise that is
-    // passed to React (via `use`) so that the latency is accurately represented
-    // in the React DevTools.
-    const stateWithDebugInfo = useMemo(()=>{
-        if (!__DEV__) {
-            return state;
-        }
-        if (isThenable(state)) {
-            // useMemo can't be used to cache a Promise since the memoized value is thrown
-            // away when we suspend. So we use a WeakMap to cache the Promise with debug info.
-            let promiseWithDebugInfo = promisesWithDebugInfo.get(state);
-            if (promiseWithDebugInfo === undefined) {
-                const debugInfo = [];
-                promiseWithDebugInfo = Promise.resolve(state).then((asyncState)=>{
-                    if (asyncState.debugInfo !== null) {
-                        debugInfo.push(...asyncState.debugInfo);
-                    }
-                    return asyncState;
-                });
-                promiseWithDebugInfo._debugInfo = debugInfo;
-                promisesWithDebugInfo.set(state, promiseWithDebugInfo);
-            }
-            return promiseWithDebugInfo;
+    // Work around React 19's `use(thenable)` hook-count bug
+    // (facebook/react#33556, vercel/next.js#78396) by reading any pending
+    // thenable via a tiny useState/useEffect wrapper instead of React's `use`.
+    // This keeps the hook count stable across renders. Suspense fallbacks are
+    // slightly less immediate during navigation, but the app no longer crashes
+    // with "Rendered more hooks than during the previous render".
+    return useUnwrapState(state);
+}
+function useUnwrapState(_state) {
+    const [state, setState] = useState(_state);
+    useEffect(() => {
+        if (isThenable(_state)) {
+            _state.then(setState);
+        } else {
+            setState(_state);
         }
-        return state;
     }, [
-        state
+        _state
     ]);
-    return isThenable(stateWithDebugInfo) ? use(stateWithDebugInfo) : stateWithDebugInfo;
+    return state;
 }
 
 //# sourceMappingURL=use-action-queue.js.map
